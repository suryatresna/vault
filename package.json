{
  "name": "go-admin",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "5.10.0",
    "npm": "3.5.0"
  },
  "scripts": {
    "build:dev:client": "(cd client && npm run build:dev:client --silent)",
    "build:dev:server": "(cd client && npm run build:dev:server --silent)",
    "hot-assets": "(cd client && npm run hot-assets)",
    "rails-server": "echo 'render hot module for client' && foreman start -f Procfile.hot"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "cacheDirectories": [
    "client/node_modules"
  ]
}
